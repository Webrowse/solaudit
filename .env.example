# Required: set to 1 to enable the devnet integration test
SOLAUDIT_DEVNET_TEST=1

# Required: program ID printed by `solana program deploy target/deploy/counter.so`
COUNTER_PROGRAM_ID=

# Required for CI: base64-encoded devnet keypair JSON
# Generate with: solana-keygen new --no-bip39-passphrase -o /tmp/ci-keypair.json && base64 < /tmp/ci-keypair.json
SOLANA_DEVNET_KEYPAIR=

